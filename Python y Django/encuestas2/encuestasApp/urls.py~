from django.urls import path

from . import views

urlpatterns = [
	path('', views.index),
	path('login', views.login),
	path('registrarse', views.registrarse),
	path('verEncuesta', views.verEncuesta),
	path('crearEncuesta', views.verEncuesta),
]
